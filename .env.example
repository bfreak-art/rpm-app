# Supabase (optional — leave empty to run in local-only mode on one device)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Calendar sync (optional — OAuth Client ID from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=
